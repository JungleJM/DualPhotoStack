# Semaphore Configuration Environment Variables
# Copy this file to .env and update the values as needed

# Admin User Configuration
SEMAPHORE_ADMIN_PASSWORD=changeme123!
SEMAPHORE_ADMIN_EMAIL=admin@localhost

# Security Configuration
# Generate a secure random base64 string for access key encryption
# You can generate one with: openssl rand -base64 32
SEMAPHORE_ACCESS_KEY_ENCRYPTION=ChangeThisToASecureRandomBase64String

# Optional Email Configuration (uncomment and configure if needed)
# SEMAPHORE_EMAIL_HOST=smtp.gmail.com
# SEMAPHORE_EMAIL_PORT=587
# SEMAPHORE_EMAIL_USERNAME=your-email@gmail.com
# SEMAPHORE_EMAIL_PASSWORD=your-app-password
# SEMAPHORE_EMAIL_SECURE=false

# Optional Web Configuration (uncomment if using reverse proxy)
# SEMAPHORE_WEB_HOST=semaphore.yourdomain.com
# SEMAPHORE_WEB_ROOT=/semaphore